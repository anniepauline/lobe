{
  "name": "@lobe/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "benchmark:vector": "bun --env-file=../../.env run scripts/benchmark-vector-search.ts",
    "build": "tsc -p tsconfig.json",
    "db:generate": "bun --env-file=../../.env run drizzle-kit generate",
    "db:migrate": "bun --env-file=../../.env run drizzle-kit migrate",
    "db:seed": "bun --env-file=../../.env run scripts/seed-demo.ts",
    "db:studio": "bun --env-file=../../.env run drizzle-kit studio",
    "test": "bun test",
    "test:db": "RUN_DB_TESTS=1 bun --env-file=../../.env test src/repositories.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@lobe/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
